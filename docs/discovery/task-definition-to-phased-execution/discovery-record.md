# Discovery Record: Task Definition to Phased Execution

Status: `ready-for-handoff` · Task: `task-definition-to-phased-execution` · Session: `1`<br>
Created: `2026-07-21T22:02:58-03:00` · Last updated:
`2026-07-21T22:57:15-03:00`

## Current state

Discovery is complete for the explicit-only `phased-plan-to-goal` skill. The
handoff defines evidence-guided intake, normalized definition and plan
generation, coherent versioned artifacts, single-phase and two complete-plan
authorization modes, immutable safety stops, controlled consequential actions,
verified phased execution, durable resumption, and honest closure.

- Next unresolved point: None - readiness review passed
- Primary handoff: `task-definition.md`
- Research notes: [research-notes.md](research-notes.md)
- Latest snapshot: [versions/task-definition-v001.md](versions/task-definition-v001.md)

## Canonical decision trail

| Time | Type | Question or event and why it mattered | Recommendation or evidence | Answer, decision, or definition impact |
| --- | --- | --- | --- | --- |
| 2026-07-21T22:02:58-03:00 | Session start | The user requested discovery for a skill that consumes task-definition documentation, produces a detailed plan, asks questions when necessary, waits for approval, and updates documentation. This established the desired outcome but left the workflow open for refinement. | Begin with the control and authorization model because it constrains every later workflow decision. | Discovery started; implementation is explicitly excluded during this phase. |
| 2026-07-21T22:02:58-03:00 | Naming decision | The initial name `task-definition-to-phased-plan` understated that the skill must execute the approved plan, not merely produce it. | Use `task-definition-to-phased-execution` to represent the full lifecycle. | The user confirmed `task-definition-to-phased-execution` and approved `docs/discovery/task-definition-to-phased-execution/`. |
| 2026-07-21T22:02:58-03:00 | Discovery question | What precise approvals are required before implementation begins and between phases? This matters because approval is an authorization boundary, not merely a presentation preference. | Support approval at either complete-plan or single-phase scope, with the authorization boundary stated explicitly. | Approval of the complete plan authorizes all phases and leaves ordinary gate decisions to the agent. Approval of only one phase requires the agent to stop before the next phase and request approval or initiate review, debate, discussion, or brainstorming based on changed circumstances and remaining work. |
| 2026-07-21T22:05:52-03:00 | Discovery question | When does a discovery or deviation during implementation exceed the authority granted by full-plan approval? Without this rule, the agent could either overreach or interrupt harmless execution unnecessarily. | Offer both a change-sensitive approval mode and a persistent long-running mandate, selected explicitly when execution is authorized. | Both options must be available. When documentation is sufficiently refined and the user trusts the agent with a long task, the agent must be able to continue working toward the goal through plan changes without repeatedly stopping for approval. |
| 2026-07-21T22:08:03-03:00 | Discovery question | Which boundaries remain immutable under a persistent mandate? This matters because continuous execution should not silently become unlimited authority. | Permit autonomous replanning inside the approved objective and explicitly declared mutation surface, but still stop for unavailable credentials or permissions, destructive or irreversible actions not already authorized, external side effects beyond scope, conflicting user instructions, or an infeasible objective. | The user confirmed all recommended mandatory stop conditions. These remain hard boundaries even under a persistent mandate. |
| 2026-07-21T22:12:46-03:00 | Discovery question | Which document is canonical for execution state, and may the supplied task definition be rewritten? This affects traceability, resumability, and the risk of erasing the original contract while implementation evolves. | Preserve source provenance, derive a normalized definition, and keep mutable execution state separate. | The skill must generate a new detailed task definition from the received material, then create a separate implementation plan and progress document that it updates continuously. The received material is an input source, not the mutable execution record. |
| 2026-07-21T22:15:13-03:00 | Discovery question | Must the generated detailed task definition be approved before the detailed plan is produced, or can both artifacts be reviewed and approved together? This changes the number of mandatory interaction gates and whether planning may rely on an unaccepted contract. | Generate both as drafts after resolving blocking questions, then use one review gate that approves the normalized definition, the plan, the authorization scope, and the execution mode together; allow a separate definition-only gate when the user requests it or a foundational unresolved decision would make detailed planning wasteful. | Combined definition-and-plan review is the default. A definition-only gate remains an exceptional path when explicitly requested or when the contract must be stabilized before useful planning. |
| 2026-07-21T22:19:28-03:00 | Discovery question | Where should the generated task definition and implementation plan/progress record be stored? A fixed path may conflict with host-repository conventions, while an unspecified path weakens resumability and discovery. | Prefer an explicit destination supplied by the user or source task material; otherwise detect applicable repository conventions, and only then use the portable fallback `docs/tasks/<task-name>/`. Never overwrite the received source. | The user confirmed the recommended precedence and stable filenames `task-definition.md` and `implementation-plan.md`. |
| 2026-07-21T22:22:42-03:00 | Research | The canonical `skills/` catalog currently contains `task-discovery`, whose output includes a self-contained task definition, but no execution skill. This new skill can consume that output without requiring it as the only input format. | Verified by read-only inspection of the canonical skill catalog. Design the intake contract to preserve provenance and remain usable with other task-definition sources. | The relationship to `task-discovery` is complementary rather than a hard dependency. |
| 2026-07-21T22:22:42-03:00 | Discovery question | Which forms of received material should be accepted? Restricting intake to one local file improves simplicity but excludes pasted definitions or multi-document briefs; accepting arbitrary material without provenance makes later reconciliation unreliable. | Accept one or more local files, directories, and/or text supplied in the conversation; identify sources and precedence in the generated definition; ask which source governs only when material conflicts cannot be resolved from instructions or context. | The user confirmed all forms and expects a folder or path to be the usual reference input. |
| 2026-07-21T22:23:58-03:00 | Discovery question | How should the skill inspect a reference directory? Unbounded recursive reading can waste time, absorb generated or irrelevant files, and expose unnecessary sensitive content; shallow reading can miss material constraints. | Use evidence-guided traversal: first inventory the path and applicable instructions, prioritize task definitions, indexes, manifests, and linked sources, expand only when needed, skip VCS metadata and generated or binary content unless material, and record omitted areas or access limits. | The user confirmed the evidence-guided traversal strategy. |
| 2026-07-21T22:27:45-03:00 | Discovery question | Should material clarification questions be asked one at a time or bundled? Bundling can reduce turns but hides dependencies between answers; asking every minor question separately can make planning unnecessarily slow. | Research first, make safe explicit assumptions for non-material gaps, and ask exactly one focused question per turn only when its answer could materially alter the definition, plan, authorization, risk, or verification. | The user confirmed the one-material-question-per-turn rule. |
| 2026-07-21T22:28:58-03:00 | Discovery question | How should a continuously rewritten implementation plan retain history? A current-only document is easy to read but can erase authorization and deviation evidence; an append-only document becomes difficult to operate from. | Keep one operational document with a concise current plan and status plus one canonical chronological execution trail; snapshot the full document only at initial approval, material replanning, authorization-mode changes, and closure. | The user confirmed this model with an additional coherence constraint: repeated updates must not degrade the document into careless line-by-line accumulation. Current-state content must be deliberately rewritten so the whole artifact continues to read coherently. |
| 2026-07-21T22:31:00-03:00 | Discovery question | Which events require a document update? Updating after every command creates noise and churn; updating too rarely weakens recovery and trust. | Rewrite after approval or authorization changes, phase start, phase completion or failure, material decisions or deviations, blocker transitions, material replanning, pause or resume, and closure. Do not record routine commands or transient observations unless they become material evidence. | The user confirmed the recommended lifecycle-event cadence and coherent whole-document rewrite requirement. |
| 2026-07-21T22:33:31-03:00 | Discovery question | What qualifies as a phase? Arbitrarily small phases create approval and documentation overhead; overly broad phases hide risk and make verification or recovery ambiguous. | Make each phase outcome-led, bounded, dependency-aware, and independently verifiable, with explicit entry conditions, expected mutations, checks, completion evidence, and rollback or contingency where material. Use as many phases as the work requires, without arbitrary duration or count targets. | The user confirmed the recommended phase contract. |
| 2026-07-21T22:34:26-03:00 | Discovery question | What happens when a phase check fails? Treating attempted work as completion corrupts status, while stopping on the first recoverable failure defeats autonomous execution. | Keep the phase incomplete; diagnose and iterate within the authorized phase. Under a persistent mandate, replan and continue within immutable boundaries. Under change-sensitive approval, pause only if recovery is a material change. Mark blocked only when no safe authorized path remains, and record failed evidence and the exact resolver or next action. | The user confirmed the recommended failure policy. A limitation can count as completion only when explicitly accepted and documented. |
| 2026-07-21T22:35:38-03:00 | Discovery question | Does approving a complete plan authorize consequential external or durable actions? Generic approval can be ambiguous for commits, pushes, deployments, database mutations, destructive operations, purchases, or communications. | Require the plan to enumerate its mutation surface. Approval authorizes disclosed reversible local edits and checks. Consequential actions require explicit named inclusion and authorization; if named in the approved plan they may proceed, otherwise the agent must stop and request authority. | The user confirmed this authorization rule. Complete-plan approval covers a consequential action only when the approved plan names it explicitly. |
| 2026-07-21T22:36:55-03:00 | Discovery question | How should execution resume after interruption or in a fresh context? Trusting stale document status can overwrite user work or repeat side effects; rediscovering everything wastes time and can lose prior authority. | Reread applicable instructions, the generated definition, current plan/progress document, latest snapshot, and material source references; inspect actual workspace or external state read-only; reconcile drift and record a resume event. Continue under existing authorization only if the objective, mutation surface, execution mode, and next action remain valid. | The user confirmed the recommended resumption contract. Material conflict between recorded and actual state requires clarification or renewed approval. |
| 2026-07-21T22:39:32-03:00 | Discovery question | What evidence permits final closure? Finishing commands or phases is insufficient if required checks failed, the documents are stale, or consequential side effects remain unclear. | Close only after all phases reach a terminal state, required checks pass or limitations are explicitly accepted, actual state is reconciled, scope and consequential actions are accounted for, the generated definition and plan are coherent, and a final snapshot plus concise handoff are produced. | The user confirmed the recommended completion and closure contract. |
| 2026-07-21T22:40:36-03:00 | Discovery question | May the skill trigger implicitly when a task document is mentioned? Because the skill writes plans and can execute approved mutations, implicit triggering could turn a request to inspect or discuss documentation into an implementation workflow. | Make activation explicit-only: the user must name or invoke the skill. After activation, planning is allowed, but implementation still requires the combined approval gate and recorded authorization scope. | The user confirmed explicit-only activation. |
| 2026-07-21T22:41:30-03:00 | Discovery question | What should the canonical skill be named? The discovery directory describes the full transformation but is unnecessarily long for repeated invocation. | Initially proposed `task-execution`. | The user prefers a goal-oriented name such as `phased-plan-to-goal`, emphasizing persistent execution toward the approved outcome rather than static checklist completion. |
| 2026-07-21T22:42:31-03:00 | Discovery question | Does `phased-plan-to-goal` accurately name the skill despite the fact that it also creates the normalized task definition and phased plan from source material? | Use `phased-plan-to-goal`; the name foregrounds the distinctive execution behavior, while the description and instructions make intake and plan generation explicit. | The user confirmed `phased-plan-to-goal` as the canonical skill name. |
| 2026-07-21T22:43:19-03:00 | Readiness review | Applied the discovery readiness checklist to identify remaining material gaps. Phase integrity and core artifact roles are coherent, but the portability boundary for uninterrupted long-running execution is not explicit, and implementation resource design remains pending until workflow closure. | The portable skill can prohibit voluntary routine stops under a persistent mandate, but it cannot guarantee that every host runtime will remain active for a fixed wall-clock duration. Durable documents and the settled resume contract must preserve progress across unavoidable runtime pauses. | Readiness remains `needs-work`; continue discovery with the runtime-persistence boundary. |
| 2026-07-21T22:43:19-03:00 | Discovery question | What promise should persistent execution make when a host runtime cannot sustain a continuous 20-hour turn? Claiming guaranteed wall-clock continuity would be false across portable Agent Skills clients. | Define persistent execution as no voluntary stop while safe authorized work remains and the runtime permits progress; require durable checkpoints and resume from reconciled state after unavoidable runtime, tool, or session interruption. | The user confirmed this portable definition of persistent execution. |
| 2026-07-21T22:54:35-03:00 | Discovery question | May the generated task definition be rewritten after approval? If normal replanning silently changes the governing objective or boundaries, the authorization record becomes unreliable; if it is permanently immutable, legitimate user-directed changes cannot be represented. | Rewrite the definition freely while it is a draft. After approval, treat it as a versioned contract: execution updates only the plan/progress document, while a goal, scope, constraint, deliverable, or completion-criteria change requires explicit user authorization, preservation of the prior version, and reconciliation of the plan. | The user confirmed the recommended task-definition versioning rule. |
| 2026-07-21T22:55:52-03:00 | Discovery question | What language counts as approval? Requiring exact commands is brittle, but interpreting discussion or praise as authorization is unsafe. | The combined review should ask one direct approval question naming the available scopes and modes. Accept ordinary natural-language approval only when it unambiguously identifies single-phase, complete-plan change-sensitive, or complete-plan persistent authorization; otherwise clarify. Never infer approval from silence, planning discussion, or general positive feedback. | The user confirmed the recommended approval-recognition contract. |
| 2026-07-21T22:57:15-03:00 | Research | Inspected the canonical `task-discovery` skill, optional Codex metadata, repository layout, and validator/test references to derive an implementation resource design. | Verified current repository conventions and recorded evidence and limitations in [research-notes.md](research-notes.md). | The handoff now names the canonical skill folder, templates, readiness reference, optional metadata, representative scenario review, and four required validation commands. |
| 2026-07-21T22:57:15-03:00 | Readiness review | Reapplied every checklist area after final synthesis. | Phase integrity: `pass`; primary handoff structure, materiality, and freshness: `pass`; record and evidence traceability: `pass`; scope, exclusions, deliverables, constraints, dependencies, risks, approval, validation, and completion: `pass`; fresh-implementer sufficiency: `pass`. | No unmanaged material unknown remains. Both documents set to `ready-for-handoff`; snapshot `versions/task-definition-v001.md` created; discovery closed without implementation. |

## Linked research notes

- [research-notes.md](research-notes.md): canonical catalog conventions,
  validation surface, and portable long-running execution boundary.

Keep this record lossless. Preserve every question, answer, evidence item,
rationale, and managed unknown in this trail or the linked notes.
