# Discovery Record: AI Second Brain

Status: `ready-for-handoff` · Task: `ai-second-brain` · Session: `1`<br>
Created: `2026-07-26T13:05:02-03:00` · Last updated:
`2026-07-26T16:25:46-03:00`

## Current state

The selected solution is a Codex-backed, Markdown-authoritative second brain
with Obsidian as an optional local view. It initially supports game
playthroughs and uses a domain-neutral core for later activities. The definition
specifies text, interpreted screenshots, transcript-only voice capture,
firsthand-only behavior, context isolation, provenance, checkpoints,
corrections, bounded initiative, retention, and session continuity.

- Next unresolved point: None; discovery is closed.
- Primary handoff: `task-definition.md`
- Research notes: [research-notes.md](research-notes.md)
- Product comparison: [solution-comparison.md](solution-comparison.md)
- Latest snapshot: [versions/task-definition-v001.md](versions/task-definition-v001.md)

## Canonical decision trail

| Time | Type | Question or event and why it mattered | Recommendation or evidence | Answer, decision, or definition impact |
| --- | --- | --- | --- | --- |
| `2026-07-26T13:05:02-03:00` | Session start | The user proposed an AI/agent second brain for game playthroughs: maintain a chat through a harness, send information found in a game, and have the AI organize it in a folder. | Treat the chat as a candidate interaction surface and the folder as a candidate durable record; do not assume either is sufficient until capture, retrieval, and continuity needs are understood. | Opened a read-only discovery to compare solutions and produce a coherent implementation definition without building or installing anything. |
| `2026-07-26T13:05:02-03:00` | Naming decision | A stable discovery name was required before creating artifacts. The first proposal, `gameplay-second-brain`, could unnecessarily constrain later reuse. | Prefer the broader name if reuse beyond games is a plausible future direction, while keeping game playthroughs as the present use case. | The user selected and confirmed `ai-second-brain`; created `docs/discovery/ai-second-brain/`. |
| `2026-07-26T13:05:02-03:00` | Discovery question | The primary capture experience changes the required integrations, latency, privacy model, multimodal capabilities, and implementation cost. | Start with deliberate user capture through chat—text plus optional screenshots or short voice notes—unless hands-free or automatic observation is a firm requirement. This provides high control and a much smaller trustworthy first system. | Asked which inputs the first usable version must support. |
| `2026-07-26T13:08:39-03:00` | User decision | The user selected typed messages, messages with screenshots, and voice capture. | Keep all three as required first-version input modes. Treat voice as captured user input, not as authorization for always-on listening, and exclude continuous screen observation unless later added explicitly. | The first version must accept text, image-bearing messages, and voice. Automatic or continuous game observation is not currently required. |
| `2026-07-26T13:08:39-03:00` | Discovery question | The execution environment determines feasible screenshot and microphone capture, background behavior, folder access, and whether a local app, web client, mobile companion, or cross-device service is appropriate. | If the games and knowledge folder are on one Windows PC, prefer a local-first Windows companion or local web UI with bounded folder access. Do not assume that topology for console or mobile play. | Asked where the user will play and interact with the second brain. |
| `2026-07-26T13:10:03-03:00` | User decision | The user will play and interact with the second brain on the same Windows PC. | Use a local-first Windows topology so capture and storage do not require a second device or remote relay. Candidate interaction mechanisms include a desktop companion or locally hosted UI with global hotkeys, clipboard or screenshot intake, and push-to-talk capture. | The first version targets one Windows PC for gameplay, interaction, capture, and the user-owned knowledge folder. Cross-device access is not required. |
| `2026-07-26T13:10:03-03:00` | Discovery question | A cloud-model prohibition would materially constrain multimodal model quality, hardware requirements, setup complexity, latency, and cost. Conversely, cloud processing sends captured game and microphone content to a provider. | Prefer local storage and bounded local tools while allowing cloud-hosted multimodal models, unless privacy, offline use, or recurring API cost makes local inference a firm requirement. | Asked whether cloud processing is acceptable. |
| `2026-07-26T13:12:50-03:00` | User constraint | The user accepts cloud processing only if it does not introduce rate limits, API limits, or comparable interruptions. | Challenge a literal no-limit cloud requirement: hosted providers document rate and/or usage limits. Separate uninterrupted local capture from immediate cloud inference, and clarify whether high paid limits plus queue/retry behavior satisfy the intent. | Cloud processing is privacy-acceptable, but the acceptable metering and interruption semantics remain unresolved. |
| `2026-07-26T13:12:50-03:00` | Research | Current official documentation was checked because provider limits are dynamic and materially constrain the architecture. | [research-notes.md](research-notes.md) records that OpenAI, Anthropic, and Gemini APIs enforce tiered rate or usage limits, while Ollama can run locally without cloud features. Evidence class: verified as of this session; account-specific capacity remains unknown. | A hosted provider cannot credibly satisfy a literal guarantee of unlimited immediate inference. Viable designs are high-limit pay-as-you-go with durable local queueing, local inference, or a hybrid. |
| `2026-07-26T13:12:50-03:00` | Discovery question | “No limits” may mean either no fixed message allowance during normal personal use or a hard requirement for unmetered, immediately available inference. Those meanings lead to different solutions. | Prefer a durable local inbox plus pay-as-you-go cloud inference and retry/backoff if the concern is interrupted capture. Require a local model fallback only if immediate AI responses must continue independently of provider quotas or outages. | Asked whether metered pay-as-you-go cloud service was acceptable. |
| `2026-07-26T13:27:28-03:00` | User constraint | The user will not use pay-as-you-go billing and already has a ChatGPT Pro subscription; that subscription is the maximum recurring AI-service spend. | Exclude ordinary token-billed APIs and additional paid AI subscriptions. Investigate subscription-authenticated OpenAI surfaces before proposing a custom API harness. | The first version must operate within ChatGPT Pro plus local, non-metered software and hardware. |
| `2026-07-26T13:27:28-03:00` | Research | Current OpenAI plan documentation was checked to determine what Pro can support without API billing. | [research-notes.md](research-notes.md) records that Pro supports Codex local clients and scriptable workflows but provides higher finite usage limits, not unlimited Codex inference. ChatGPT Voice has its own plan allowance and still draws task work from the Codex budget. Evidence class: verified from current official documentation. | A ChatGPT/Codex-based harness is viable within the subscription, but cannot guarantee unlimited model responses. A local capture queue remains required; a local model fallback is required only if subscription-limit interruptions are unacceptable. |
| `2026-07-26T13:27:28-03:00` | Discovery question | The user's cost ceiling is now clear, but it conflicts with a literal no-limit cloud requirement. Whether Pro's included limits are tolerable determines if Codex alone is acceptable or a local fallback is mandatory. | Prefer ChatGPT desktop/Codex as the first harness if the included Pro capacity is acceptable for ordinary play; retain local durable capture so hitting a limit never loses information. | Asked whether the Pro plan's existing usage windows satisfy the intent. |
| `2026-07-26T13:29:18-03:00` | User clarification | ChatGPT Pro's own usage limits are acceptable. The rejected limit is an additional free-tier or hosted-service quota imposed by a plugin, MCP server, skill dependency, transcription service, or similar component. | Treat ChatGPT Pro as the one permitted hosted inference dependency. Require all added capabilities to be local and non-metered unless the user later approves an exception. Distinguish a local skill or local MCP process from a hosted plugin service. | A local fallback model is not required merely to bypass Pro limits. Third-party hosted quota dependencies are out of scope; local skills, scripts, file tools, and locally running MCP processes are eligible. |
| `2026-07-26T13:29:18-03:00` | Research | Official OpenAI documentation was checked to verify which local extension and input surfaces avoid a second hosted quota layer. | [research-notes.md](research-notes.md) records that skills are reusable instruction packages, a stdio MCP server is a locally launched process, Codex app-server can support a custom local client, and desktop voice dictation transcribes into the composer. Evidence class: verified from current official documentation. | Both a direct Codex desktop workflow and a custom local companion backed by Codex are technically plausible without another paid service. The custom companion adds implementation and maintenance cost, not a service quota. |
| `2026-07-26T13:29:18-03:00` | Discovery question | Choosing the visible harness determines whether the task is mostly a local skill and folder contract or also includes a custom Windows UI, hotkeys, app-server integration, capture queue, and lifecycle management. | Start with the ChatGPT/Codex desktop app itself unless switching windows materially disrupts gameplay. It already supplies subscription authentication, local project access, image inputs, and voice dictation. | Asked whether the existing desktop interface is acceptable. |
| `2026-07-26T13:38:37-03:00` | User decision | The first version may use the Codex app directly. | Select a dedicated local Codex project as the harness. Add a purpose-built skill and local knowledge-folder contract; do not build an overlay or app-server client unless later evidence shows the direct workflow is inadequate. | Custom Windows UI, global hotkeys, app-server integration, and companion lifecycle management are out of scope for the first version. |
| `2026-07-26T13:38:37-03:00` | Discovery question | External guides or web search could fill gaps but may introduce spoilers, contaminate the user's firsthand record, and make provenance harder to trust. Prohibiting all external information could limit help when the user explicitly wants it. | Default to user-provided evidence only. If external research is desired, require an explicit request and keep imported information clearly separated from firsthand playthrough knowledge. | Asked for the permitted external-information and spoiler boundary. |
| `2026-07-26T15:06:37-03:00` | User decision | Both modes are required. By default, the system must remain completely restricted to information the user provides. Even when the model knows a game fact from training, it must behave as if it does not know until the user's accumulated evidence is sufficient. It may use outside or latent knowledge only when the user asks. | Implement a strict default epistemic firewall: do not reveal, confirm, deny, hint at, or use latent/external game knowledge in reasoning presented to the user. Treat explicit outside-knowledge access as a narrowly scoped override and preserve provenance separation. | Default playthrough assistance is grounded exclusively in the user's record. Explicit requests may unlock model knowledge or research for that request, but outside material must not silently become firsthand truth. |
| `2026-07-26T15:06:37-03:00` | Discovery question | Updating synthesized files after every message provides immediate structure but increases latency, file churn, and model usage. Deferring all writes risks losing raw observations and leaves the folder stale. | Persist every input immediately in a lossless inbox, then reconcile organized knowledge at explicit checkpoints or when the user asks a question that requires it. This separates durable capture from synthesis and preserves corrections. | Asked the user to choose between immediate rewriting and checkpoint synthesis. |
| `2026-07-26T15:10:26-03:00` | User decision | After reviewing the tradeoff, the user approved the hybrid capture policy. | Append every incoming capture immediately to a lossless session inbox. Reconcile synthesized knowledge when the user asks to organize, before answering a question that needs cross-session state, and when ending a play session. Process corrections and material state changes immediately. | Durable capture and knowledge synthesis are now separate operations with explicit reconciliation triggers. |
| `2026-07-26T15:10:26-03:00` | Discovery question | A flat folder can accidentally mix facts across games, save files, characters, difficulty modes, or repeat playthroughs. A deeply generic hierarchy may add needless friction if only one active run matters. | Use a separate game identity and playthrough identity when multiple or repeat runs are expected; keep shared game-level knowledge distinct from run-specific discoveries. | Asked whether the folder must support multiple games and repeat playthroughs. |
| `2026-07-26T15:19:33-03:00` | User decision | The user approved a low-friction default in which each game would have a `main` playthrough and additional isolated playthroughs only on request. | Preserve the underlying isolation behavior while keeping the initial folder experience simple. | Provisional game-oriented hierarchy accepted. |
| `2026-07-26T15:20:00-03:00` | Scope correction | The user clarified that the second brain may be used for activities other than games. This makes `games/<game>/playthroughs/<run>` too narrow as the governing storage model. | Generalize the core to neutral containers and implement game playthroughs as one profile rather than as the root taxonomy. Preserve isolated contexts and explicit sharing across all profiles. | Reopened the hierarchy naming decision. The system must support additional activity profiles without restructuring the vault or weakening provenance boundaries. |
| `2026-07-26T15:20:00-03:00` | Discovery question | Neutral names must be understandable in daily use while expressing two distinct concepts: a broad subject and an isolated body of accumulated evidence. | Use `collections/<subject>/contexts/<context>`, with `main` created automatically when only one context exists. A game is a collection, a playthrough is a context, and other profiles can define equivalent meanings. | Asked whether to adopt the generic vocabulary. |
| `2026-07-26T15:21:39-03:00` | User decision | The user approved `collections/<subject>/contexts/<context>` as the generic structure. | Create `main` automatically for the first context. Require explicit creation and explicit cross-context sharing for additional contexts. Activity profiles define friendly domain meanings without changing these core paths. | The generic container hierarchy is settled. |
| `2026-07-26T15:21:39-03:00` | Discovery question | A single long-running chat is convenient but accumulates context, hidden compaction, and stale assumptions. Starting fresh chats proves that the folder is authoritative but adds a small resume step. | Use one chat during a live activity session, then allow a fresh chat to resume the same context from its files at the next session. Do not make continued access depend on hidden chat history. | Asked whether to adopt a session-based chat lifecycle. |
| `2026-07-26T15:23:12-03:00` | User decision | The user approved one Codex chat per activity session, with later sessions able to start fresh and resume from durable context files. | On session start, hydrate only from the selected context's current state and necessary recent evidence. On session end, reconcile the inbox and write a durable checkpoint. Treat chat history as non-authoritative working context. | Session continuity must work without resuming the same Codex chat. |
| `2026-07-26T15:23:12-03:00` | Discovery question | A Markdown-only vault is transparent and editable but needs disciplined event identifiers and links. A database or vector store adds query power but reduces portability and introduces migrations and repair obligations before scale proves it necessary. | Use Markdown as the authoritative human-readable format, ordinary media files for screenshots, and a small append-only structured event ledger only where lossless capture requires it. Do not require SQLite or a vector database in the first version; permit rebuildable local indexes later. | Awaiting the durable-format choice. |
| `2026-07-26T15:31:02-03:00` | User decision | The user approved the recommended Markdown-first, database-free storage model. | Keep ordinary Markdown and media files authoritative. Use stable capture identifiers and append-only records where lossless intake needs structure. Any future index must be local, disposable, and rebuildable from the files. | SQLite, a vector database, and hosted search are not required in the first version. |
| `2026-07-26T15:31:02-03:00` | Discovery correction | The user challenged why other products had not been suggested. The earlier acceptance that Codex *can* be used directly had been incorrectly promoted into a final product selection without a fair comparison. | Reopen the harness decision. Compare products in two lanes: ChatGPT Pro as the reasoning engine, and fully local inference with no hosted quota. Preserve settled requirements independently of the product choice. | Codex is now a leading candidate rather than a selected harness. The supporting comparison covers Codex, Codex plus Obsidian, LM Studio, AnythingLLM Desktop, and Open WebUI with a local model runtime. |
| `2026-07-26T15:33:13-03:00` | Research | Current official product documentation was reviewed because capabilities, pricing, storage, and local-operation claims can change. | [solution-comparison.md](solution-comparison.md) records a source-backed comparison. Obsidian is a local Markdown companion rather than an AI engine. LM Studio is a local model runtime and chat app. AnythingLLM Desktop and Open WebUI are fuller local AI workspaces but make their own application-managed stores central. | The provisional preference is Codex plus an optional Obsidian view over the same Markdown folder when ChatGPT Pro should remain the model. AnythingLLM Desktop is the strongest simple all-local alternative; Open WebUI is the strongest more configurable self-hosted alternative. |
| `2026-07-26T15:33:13-03:00` | Discovery question | The largest remaining product discriminator is not UI polish but model source. ChatGPT Pro gives stronger hosted reasoning within the already accepted native allowance; local models eliminate hosted inference quotas but consume PC resources and may reduce reasoning or vision quality. | Keep ChatGPT Pro unless avoiding even its accepted native limits is worth switching to local inference. Product selection should follow that choice rather than precede it. | Asked whether ChatGPT Pro should remain the reasoning engine or whether fully local models are acceptable as the primary engine. |
| `2026-07-26T15:44:03-03:00` | Product comparison | The user asked whether Codex plus Obsidian is better than Codex alone, and whether Notion or Linear impose additional limits on Codex calls. | Recommend the Codex-backed Markdown architecture with Obsidian as an optional local view/editor. It adds no AI call or remote API layer and can be introduced or removed without changing the authoritative files. Notion and Linear are remote integrations with their own API rate limits; Linear is primarily task tracking rather than the governing knowledge store. | The comparison is now focused on the Codex-backed lane. Selection of the optional Obsidian view remains pending explicit confirmation. |
| `2026-07-26T15:44:03-03:00` | Research | Current OpenAI, Notion, and Linear documentation was checked to separate model usage from external tool-call limits. | Codex reasoning continues to consume the user's ChatGPT/Codex plan allowance regardless of the storage tool. Notion documents per-connection and per-workspace API rate limits. Linear documents request and complexity rate limits for its API. OpenAI documents that Codex for Linear creates a Codex cloud chat; no separate Linear-specific Codex model-call allowance was published in the reviewed sources. | Notion and Linear do not make Codex inference independently unlimited. Their API limits are a second operational boundary, although ordinary interactive use is unlikely to approach the documented throughput. |
| `2026-07-26T15:47:41-03:00` | User decision | The user approved Codex plus optional Obsidian over the same authoritative Markdown folder. | Keep Codex fully usable without Obsidian. Obsidian is a removable local view/editor only: no AI plugin, Sync service, hosted connector, or required Obsidian-specific metadata. | The selected harness is Codex authenticated through ChatGPT Pro. The selected optional knowledge interface is local Obsidian. Both operate on the same user-owned folder, and Codex remains functional when Obsidian is closed or uninstalled. |
| `2026-07-26T15:47:41-03:00` | Discovery question | The system can either wait for explicit requests or behave more like a second brain by proactively identifying connections, conflicts, forgotten tasks, and hypotheses. Excessive initiative may interrupt the activity or inadvertently steer the user. | Prefer bounded initiative: capture silently, surface urgent contradictions or explicit reminders immediately, and collect non-urgent connections for checkpoints unless the user asks for analysis. All proactive observations must remain inside the firsthand-only evidence boundary. | Asked the user to choose the desired level of assistant initiative. |
| `2026-07-26T15:59:06-03:00` | User decision | The user approved bounded initiative. | Surface urgent contradictions, safety-relevant conflicts, and explicitly time-sensitive reminders immediately. Save non-urgent connections, patterns, forgotten threads, and hypotheses for the next checkpoint unless the user requests analysis sooner. Never use initiative to bypass firsthand-only mode. | Bounded initiative is now part of the behavioral contract. |
| `2026-07-26T15:59:06-03:00` | Discovery question | The authoritative Markdown decision still requires a concrete default file contract. Too few files create unmanageable monoliths; too many categories create friction and force every activity into a rigid taxonomy. | Start every context with a compact generic set: metadata and current state, lossless session inbox, open items and hypotheses, chronological timeline, on-demand topic notes, attachments, and a separate area for explicitly requested outside knowledge. Let activity profiles add optional views without changing the core. | Asked whether to adopt the proposed compact context file set. |
| `2026-07-26T16:00:49-03:00` | User clarification | The user asked what “activity profiles” means. | Define it as an optional local template that gives the generic core domain-friendly vocabulary, suggested note types, and checkpoint views. It is not a user account, AI model, hosted service, plugin dependency, or separate data store. Recommend the clearer name `activity template`. | The compact file-set decision remains pending while terminology is clarified. Asked whether to rename the concept. |
| `2026-07-26T16:01:43-03:00` | User decision | The user accepted the clarification and proposed terminology. | Use `activity template` in the synthesized definition. Preserve earlier references to `activity profile` in this lossless trail as the historical wording that led to the renamed concept. | The optional domain-specific organizational layer is now named `activity template`. Returned to the pending compact file-set decision. |
| `2026-07-26T16:04:08-03:00` | User decision | The user approved the proposed compact default file structure for every context. | Create only the generic files and folders required for durable operation: `context.md`, `timeline.md`, `open-items.md`, `inbox/`, `topics/`, `attachments/`, and `external/`. Activity templates may suggest optional topic notes but must not replace or rename the core. | The per-context storage topology is settled. |
| `2026-07-26T16:04:08-03:00` | Discovery question | Voice capture can mean preserving only an editable transcript or retaining the original audio as evidence. Raw-audio retention requires a recording path, more storage, privacy controls, and potentially custom capture beyond native Codex dictation. | Prefer transcript-only in the first version. Preserve the corrected transcript, capture time, input type, and capture ID in the inbox. Add raw audio later only if replay or transcription auditing proves necessary. | Asked whether the transcript is sufficient or raw audio must also be retained. |
| `2026-07-26T16:05:51-03:00` | User decision | The user approved transcript-only voice capture for the first version. | Use Codex's native foreground dictation. Persist the resulting editable/corrected transcript, timestamp, voice input type, and capture ID. Do not retain raw microphone audio or introduce local recording software. | Voice capture no longer requires a custom recorder or audio-retention lifecycle. |
| `2026-07-26T16:05:51-03:00` | Discovery question | Screenshots submitted through chat may originate from temporary clipboard captures or external locations. Referencing those paths can leave broken evidence after files are moved or deleted; copying every image consumes additional storage. | Copy each accepted screenshot into the active context's `attachments/` folder using its capture ID. Preserve the user's caption separately from AI visual observations so inference is not mistaken for firsthand wording. | Asked whether submitted screenshots should be copied into the context for durable retention. |
| `2026-07-26T16:07:00-03:00` | User decision and clarification | The user approved durable screenshot copies and added that screenshots should also be interpreted. | Preserve three distinct layers: the immutable image and user caption as evidence; visible text, objects, and UI state as AI observations; and relationships or meaning proposed by the AI as attributed inference with confidence. Link every layer by capture ID. Use observations and supported inferences during reconciliation, while enforcing firsthand-only mode. | Screenshot retention is settled. Asked whether to adopt the layered screenshot-interpretation contract. |
| `2026-07-26T16:09:42-03:00` | User decision | The user approved the layered screenshot-interpretation contract. | Preserve immutable image evidence, AI observations, and attributed AI inferences as distinct capture-linked layers. Promote only supported content during reconciliation, retain confidence, and never fill visual gaps using outside or latent knowledge in firsthand-only mode. | Screenshot capture and interpretation behavior are settled. |
| `2026-07-26T16:09:42-03:00` | Discovery question | A later capture may explicitly correct an earlier fact, describe a state that changed over time, or ambiguously contradict the record. Treating all three cases as overwrite or as permanent conflict would corrupt either current truth or history. | Apply explicit corrections immediately while retaining superseded history; record genuine state changes as timeline transitions; preserve both sides of an ambiguous conflict, mark the current state uncertain, and ask the user instead of guessing. | Asked whether to adopt the proposed conflict-resolution policy. |
| `2026-07-26T16:10:45-03:00` | User decision | The user approved the three-way conflict-resolution policy. | Explicit corrections immediately supersede current knowledge without deleting history. Genuine state changes become timeline transitions. Ambiguous conflicts preserve both claims, mark current knowledge uncertain, and require user resolution. | Conflict and correction behavior are settled. |
| `2026-07-26T16:10:45-03:00` | Discovery question | A fresh chat must identify exactly one active collection/context before hydrating or writing. Always asking adds friction; silently choosing can load or modify the wrong isolated context. | Resume the most recently active context by default, announce the selection at session start, and require confirmation only when the user's opening input names another subject, multiple matches are plausible, or the last context is unavailable. | Asked whether to adopt the proposed active-context selection policy. |
| `2026-07-26T16:11:38-03:00` | User decision | The user approved the active-context selection policy. | Resume the most recently active context, announce it before substantive work, and ask only when the opening input conflicts with that choice, multiple matches are plausible, or the prior context is unavailable. Never hydrate or write across an unresolved context ambiguity. | Fresh-session context selection is settled. |
| `2026-07-26T16:11:38-03:00` | Discovery question | Always displaying detailed capture provenance makes answers auditable but noisy. Hiding it entirely makes it harder to distinguish grounded recall from inference and to inspect mistakes. | Use compact provenance by default for factual recall and inference: cite the supporting capture IDs or topic note, label uncertainty, and offer full evidence expansion on request. Do not clutter simple capture acknowledgements with citations. | Asked whether to adopt the proposed answer-provenance policy. |
| `2026-07-26T16:17:48-03:00` | User decision | The user approved compact answer provenance. | Factual recall and AI inference cite compact capture IDs or topic-note references and label uncertainty. Full evidence can be expanded on request. Simple capture acknowledgements remain citation-free. | Answer provenance behavior is settled. |
| `2026-07-26T16:17:48-03:00` | Discovery question | The inbox is lossless evidence, but indefinite storage consumes disk and may retain content the user later wants removed. Automatic cleanup risks silently destroying provenance; immediate permanent deletion risks removing referenced evidence. | Never delete automatically. Archive inactive contexts without changing their contents. Require an explicit user request and confirmation before permanent deletion, show affected captures/attachments/derived knowledge, and remove or repair dangling references as one bounded operation. | Asked whether to adopt the proposed retention and deletion-safety policy. |
| `2026-07-26T16:20:28-03:00` | User decision | The user approved the retention and deletion-safety policy. | Never delete captures automatically. Archive inactive contexts without mutation. Before permanent deletion, require an explicit request and confirmation, show affected evidence and derived knowledge, then remove or repair dangling references as one bounded operation. | Retention, archive, and deletion safety are settled. |
| `2026-07-26T16:20:28-03:00` | Readiness review | A definition-only cold-read preparation exposed a contradiction between the selected direct-Codex harness and the requirement to persist every input locally before any model call. Codex cannot execute the folder write until after it receives the message and begins the agent turn. | Either relax the guarantee to “first agent action after Codex accepts the message,” with pending input recoverable through the app, or add a separate local capture component that writes before submission. The latter conflicts with the selected smallest first version and custom-client non-goal. | Readiness result: `needs-work`. Closure paused. Asked the user to choose between first-action persistence and a separate pre-model capture component. |
| `2026-07-26T16:25:46-03:00` | User decision | The user accepted first-action persistence inside Codex and the limitation that messages Codex never accepts are outside the durability guarantee. | Keep the direct Codex architecture. Do not add a pre-model capture companion or local-model fallback. Make inbox persistence the first agent action after an accepted message and expose later processing failures as pending/retryable. | The persistence contradiction is resolved. |
| `2026-07-26T16:25:46-03:00` | Research and managed unknown | Official Codex documentation confirms image attachments but does not establish that every desktop attachment is exposed as a copyable local path. | Treat local screenshot copying as an implementation verification gate. If unavailable, use the safe save-first fallback: place the image in the context's `attachments/` folder before referencing it in Codex, and leave the capture pending until durable evidence exists. Evidence class and source are recorded in [research-notes.md](research-notes.md). | The unknown is managed and no longer blocks handoff. |
| `2026-07-26T16:25:46-03:00` | Readiness checklist | Applied the required semantic review and definition-only cold read. | `Phase integrity`: pass. `Primary handoff`: pass. `Contract depth and authority`: pass. `Supporting record and evidence`: pass. `Implementation readiness`: pass, with the screenshot-path gate and fallback explicitly managed. `Definition-only cold read`: pass; a fresh agent can explain the outcome and boundaries, preserve the selected Codex/Markdown/Obsidian architecture and firsthand-only invariant, reject API-billed, hosted-quota, chat-history-only, cross-context, database-authoritative, or custom-companion substitutions, and follow the research link. | All applicable readiness areas pass. |
| `2026-07-26T16:25:46-03:00` | Closure | No material uncertainty, contradiction, missing acceptance criterion, unclear authority boundary, or unmanaged risk remains. | Mark both primary artifacts `ready-for-handoff` and save the synthesized definition as `versions/task-definition-v001.md`. | Discovery ended without implementation, installation, commit, push, publication, or external mutation. |

## Linked research notes

- [research-notes.md](research-notes.md): current evidence about hosted-provider
  limits, local inference, and product characteristics.
- [solution-comparison.md](solution-comparison.md): fair comparison of the
  credible product shapes under the settled constraints.

Keep this record lossless. Preserve every question, answer, evidence item,
rationale, and managed unknown in this trail or the linked notes.
