interface:
  display_name: "Task Discovery"
  short_description: "Thoroughly define tasks before execution"
  default_prompt: "Use $task-discovery to investigate and define this task before any execution: [describe the task]."

policy:
  allow_implicit_invocation: false

